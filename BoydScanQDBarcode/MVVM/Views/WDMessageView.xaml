<Window x:Class="BoydScanQDBarcode.MVVM.Views.WDMessageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoydScanQDBarcode.MVVM.Views"
        mc:Ignorable="d"
        Title="WDMessageView" Height="300" Width="600" WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="borderMessage">
            <TextBlock x:Name="txtMessage" Text=" THIS IS SAMPLE MESSAGE" Grid.Row="0" TextAlignment="Center" VerticalAlignment="Center" HorizontalAlignment="Center" 
                       FontSize="30" FontFamily="Century Gothic" FontWeight="Normal" Foreground="{StaticResource PrimaryBrush}" TextWrapping="Wrap"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="OK" Width="150" Style="{StaticResource OKCloseMessageButtonStyle}" Height="50" Click="OnOkButtonClick"/>
        </StackPanel>
    </Grid>
</Window>
